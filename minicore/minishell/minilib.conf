# config generated by mini-gcc.
# Save default. The minibuf is needed for malloc, printf, and file streams.
mini_buf 1024

# define headerguards, to prevent parsing the standard header
HEADERGUARDS

# Startup function
mini_start

# optimization Flag. Os,O1,O2 might be save. O3 is known to cause sometimes trouble
OPTFLAG -Os

# (with debug info)
#OPTFLAG -g -O0

# Build minilib source
INCLUDESRC

# the ldscript to use
# Can be one of: default, onlytext, textandbss
LDSCRIPT default

# Shrink the compiled binary with shrinkelf
SHRINKELF

# generate debug info (-O0 -g). Overwrites OPTFLAG and shrinkelf
#DEBUG


# function switches. Only functions named below will be compiled

mini_access
mini_brk
mini_chdir
mini_chmod
mini_chown
mini_chroot
mini_close
mini_dup
mini_dup2
mini_dup3
mini_execve
mini_execveat
mini_fchmod
mini_fchown
mini_fcntl
mini_fork
mini_free
mini_fstat
mini_fsync
mini_ftruncate
mini_getcwd
mini_getdents
mini_getgid
mini_getitimer
mini_getpgrp
mini_getpid
mini_getppid
mini_gettimeofday
mini_getuid
mini_kill
mini_link
mini_lseek
mini_malloc
mini_mkdir
mini_mknod
mini_mount
mini_mprotect
mini_nanosleep
mini_open
mini_pipe
mini_pivot_root
mini_read
mini_readahead
mini_reboot
mini_rename
mini_rmdir
mini_rt_sigaction
mini_rt_sigreturn
mini_sendfile
mini_setgid
mini_setitimer
mini_setpgid
mini_setsid
mini_setuid
mini_signal
mini_stat
mini_symlink
mini_sync
mini_tcgetattr
mini_tcsetattr
mini_time
mini_umask
mini_uname
mini_unlink
mini_utime
mini_vfork
mini_vhangup
mini_wait
mini_wait4
mini_waitpid
mini_write
mini_prints
